# Wallpapers

Place your wallpaper files here (`.png`, `.jpg`, `.jpeg`, `.webp`).

The wallpaper script (`~/.config/hypr/scripts/wallpaper.sh`) will automatically detect and cycle through them.

**Keybind:** `Super + W` → Next wallpaper
